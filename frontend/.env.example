# Onfly - exemplo de variáveis de ambiente (não commitado com valores reais)
# Copie este arquivo para `.env.local` ou use as variantes `.env.development` / `.env.production`.
# Variáveis que precisam estar disponíveis no código cliente devem começar com VITE_

VITE_API_BASE_URL=https://api.example.com
VITE_APP_TITLE=Onfly
VITE_ENABLE_MOCK=false
VITE_MAPS_API_KEY=your_maps_api_key_here
VITE_SENTRY_DSN=

# OBS: valores sensíveis (tokens, chaves privadas) NÃO devem ser commitados.
